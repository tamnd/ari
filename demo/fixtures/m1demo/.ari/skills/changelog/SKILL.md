---
name: changelog
description: Add an entry to CHANGELOG.md in the house format
argument-hint: a one-line summary of the change
---

Add a new entry to `CHANGELOG.md` at the repo root for this change: $ARGUMENTS

Follow the house format:

- Put the entry under an `## Unreleased` heading, creating the file and the
  heading if they are missing.
- Write the entry as a single `- ` bullet in the past tense, plain language,
  no marketing.
